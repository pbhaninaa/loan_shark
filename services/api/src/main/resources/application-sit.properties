# =============================================================================
# Profile: sit (local development). Default when SPRING_PROFILES_ACTIVE is not set.
# Override DB password and JWT secret in application-local.properties (copy from application-local.example.properties).
# =============================================================================

app.frontend-url=http://localhost:5173

# Local MySQL – override spring.datasource.password in application-local.properties
spring.datasource.url=jdbc:mysql://localhost:3306/loan_shark?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Philas@12

# JWT for local dev – override jwt.secret in application-local.properties for a real secret
jwt.secret=local-dev-secret-must-be-at-least-64-characters-long-for-hs512-signing
jwt.expiration-ms=86400000
