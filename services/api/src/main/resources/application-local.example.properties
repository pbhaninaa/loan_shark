# Copy this file to application-local.properties and set your local values.
# application-local.properties is gitignored and loaded optionally.

# Database – use ONE of the two options below.

# Option A: Local MySQL only (default)
spring.datasource.url=jdbc:mysql://localhost:3306/loan_shark
spring.datasource.username=root
spring.datasource.password=Philas@12

# Option B: Same DB as Railway (run locally but connect to Railway MySQL)
# Comment out Option A above, then uncomment and set MYSQL_URL below.
# Copy value from Railway: MySQL service → Variables → MYSQL_URL.
# MYSQL_URL=mysql://user:password@host:port/database

# JWT – min 64 chars for HS512
security.jwt.secret=your-local-secret-min-64-chars-for-hs512-change-in-production
security.jwt.expiration-ms=86400000

# Mail (e.g. Gmail app password)
spring.mail.username=jaystarven@gmail.com
spring.mail.password=guuywhimnbdwjrls
app.mail.from=jaystarven@gmail.com

# Stripe (test key for local)
stripe.secret-key=sk_test_...

# Optional: frontend URL for password reset / links
app.frontend-url=http://localhost:5173
app.frontend-allowed-origins=http://localhost:5173,https://localhost:5173
