# SIT = dev local, UAT = testers deployed, prod = production. Set SPRING_PROFILES_ACTIVE on Railway (uat | prod).
spring.profiles.active=${SPRING_PROFILES_ACTIVE:sit}

# Optional local overrides (copy application-local.example.properties to application-local.properties).
spring.config.import=optional:classpath:application-local.properties
