# API base URL – backend that uses the same DB (e.g. Railway BackEnd)
# Leave unset for local dev (defaults to http://localhost:8080)
# Production / deployed frontend: set to your BackEnd public URL (no trailing slash)
# VITE_API_URL=https://your-backend.up.railway.app
