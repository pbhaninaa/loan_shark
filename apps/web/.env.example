# API base URL – Backend API (Railway when deployed)
# Local dev: leave unset (defaults to http://localhost:8080)
# Vercel: set to your Railway Backend public URL (no trailing slash)
# VITE_API_URL=https://your-backend.up.railway.app
# VITE_APP_ENV=production
